{% extends 'base.html.twig' %}

{% block title %}New Demand{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('demand_index') }}"><button class="btn btn-ghost-primary mb-3"><< Back to list</button></a>
    {% endif %}
    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('home') }}"><button class="btn btn-ghost-primary mb-3"><< Back to home</button></a>
    {% endif %}
    <div class="container">
        <div class="col-8 m-auto border p-3 shadow-lg p-5 mb-5 bg-white rounded">
            <h1 class="mb-5">Create a new Demand</h1>
            {{ include('demand/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
