{% extends 'base.html.twig' %}

{% block title %}New Company{% endblock %}

{% block body %}
    <a href="{{ path('company_index') }}"><button class="btn btn-ghost-primary mb-3"><< Back to list</button></a>

    <div class="container">
        <div class="col-8 m-auto border p-3 shadow-lg p-5 mb-5 bg-white rounded">
            <h1 class="mb-5">Create a new Company</h1>
            {{ include('company/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
